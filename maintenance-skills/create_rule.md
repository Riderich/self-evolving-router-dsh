# create_rule

创建或选择当前规则草稿：已有草稿时复用；已有 stable_revision 时复制稳定代码，保留父版本；没有已验证实现时才创建空骨架。随后 view manifest 和文件，不修改来源身份。当前激活规则同时是 few-shot，先查看相关代码与验证例子，再作增量修改。

完成时返回操作生成的 ID、事实与下一步。以工具结果为准；保留反例和正确行为，不修改可信状态或预算。
